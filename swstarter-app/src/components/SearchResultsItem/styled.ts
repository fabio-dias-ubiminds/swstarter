import styled from "styled-components";

export const StyledSearchItemContainer = styled.li`
  border-bottom: 1px solid var(--pinkish-grey);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 0;
`;

export const StyledSearchItemTitle = styled.h3`
  width: 100%;
  padding-bottom: 10px;
  font-size: 16px;
  font-weight: bold;
  font-stretch: normal;
  font-style: normal;
  line-height: normal;
  letter-spacing: normal;
  color: var(--black);
`;
